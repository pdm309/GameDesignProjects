using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CameraController : MonoBehaviour
{

    /*public PlayerController player;
    public Player2Controller player2;
    private int level;
    private float distance;
    private float distancey;
    */
    // Use this for initialization
    void Start()
    {
        /*
        player = FindObjectOfType<PlayerController>();
        player2 = FindObjectOfType<Player2Controller>();
        */
    }

    // Update is called once per frame
    void Update()
    {
        //distance = player.transform.position.x - player2.transform.position.x;
        //distancey = player.transform.position.y - player2.transform.position.y;
        //transform.position.z = Mathf.Lerp(transform.position.z, distance, distancey);
    }
}
